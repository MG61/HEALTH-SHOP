<Window x:Class="Kurs7PM.Клиент.Store"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kurs7PM.Клиент"
        mc:Ignorable="d"
        Title="Store" Height="650" Width="1100" AllowsTransparency="True" Background="Transparent"
        WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Grid>
        <Border Name="BorderLog" CornerRadius="10" Visibility="Visible" BorderThickness="0,7,0,0" Padding="1,0,0,0" Margin="0,0,-0.4,0">
            <!--Для закругления краёв-->
            <Border.Background>
                <LinearGradientBrush>
                    <!--<GradientStop Color="ForestGreen" Offset="0.0"/>-->
                    <GradientStop Color="#0e1c26" Offset="1.0"/>
                    <GradientStop Color="#294861" Offset="0.0"/>
                </LinearGradientBrush>
                <!--Градиент-->
            </Border.Background>
        </Border>

        <DataGrid x:Name="data" ColumnWidth="*" d:ItemsSource="{Binding Source=medication}" Margin="432,38,10,9.6" BorderBrush="Black" BorderThickness="2" VerticalContentAlignment="Center" AutoGeneratedColumns="DataGrid_AutoGeneratedColumns" AutoGeneratingColumn="DataGrid_OnAutoGeneratingColumn">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Корзина">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Name="dob_bus" Background="LightGreen" BorderBrush="Black" BorderThickness="1"  Content="Добавить" Tag="{Binding SelectedIndex, ElementName=data}" Click="Dob_korz" TabIndex="10"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</Window>
